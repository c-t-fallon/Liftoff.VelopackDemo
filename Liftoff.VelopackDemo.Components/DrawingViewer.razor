@inject IJSRuntime JS
@implements IAsyncDisposable

<div style="position: relative; width: @Width; height: @Height;">
    <button @onclick="ZoomToFit"
            style="position: absolute; top: 12px; right: 12px; z-index: 10;
                   padding: 6px 12px; background: #2d2d2d; color: #ccc;
                   border: 1px solid #555; border-radius: 4px; cursor: pointer;
                   font-size: 13px; font-family: monospace;"
            title="Zoom to fit all elements">
        ⊡ Zoom to Fit
    </button>
    <div id="@_containerId" style="width: 100%; height: 100%;"></div>
</div>

@code {
    [Parameter] public string Width  { get; set; } = "100%";
    [Parameter] public string Height { get; set; } = "100%";
    [Parameter] public List<DrawingLine> Lines { get; set; } = [];
    [Parameter] public List<DrawingText> Texts { get; set; } = [];

    private readonly string _containerId = $"konva-{Guid.NewGuid():N}";
    private IJSObjectReference? _module;
    private DotNetObjectReference<DrawingViewer>? _dotnetRef;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            _module = await JS.InvokeAsync<IJSObjectReference>(
                "import", "./_content/Liftoff.VelopackDemo.Components/DrawingViewer.razor.js");
            _dotnetRef = DotNetObjectReference.Create(this);
            await _module.InvokeVoidAsync("init", _containerId, _dotnetRef, Lines, Texts);
        }
    }

    private async Task ZoomToFit()
    {
        if (_module is not null)
            await _module.InvokeVoidAsync("zoomToFit");
    }

    public async ValueTask DisposeAsync()
    {
        if (_module is not null)
        {
            await _module.InvokeVoidAsync("dispose");
            await _module.DisposeAsync();
        }
        _dotnetRef?.Dispose();
    }
}
